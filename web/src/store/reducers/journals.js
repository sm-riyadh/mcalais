const initialState = []

const journals = (state = initialState, action) => {
	switch (action.type) {
		// case :
		// 	return {
		// 		...state,
		// 		[action.id]: {
		// 			isLoading: true,
		// 			downloads: null,
		// 			error: false,
		// 		},
		// 	}
		default:
			return state
	}
}

export default journals
